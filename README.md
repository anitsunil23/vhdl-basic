# vhdl-basic
The project demonstrates the implementation of basic digital logic gates using VHDL. The design was simulated using ModelSim. The project includes implementation of or gate, nor gate and aoi. The logic gate can be easily modified by changing the logical operation in the VHDL code.  
